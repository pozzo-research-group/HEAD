# Branch of BO that deals with SAXS based optimization

This is a copy of BO branch with additional tools and examples to work with SAXS data.
Consider this as the `main` branch and create a PR with the suffix `saxs_` to merge any changes.

Installation is same as the BO branch but the `require.txt` file will have packages specific to this psuedo-main branch.